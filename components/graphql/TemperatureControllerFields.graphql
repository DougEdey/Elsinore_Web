fragment TemperatureControllerFields on TemperatureController {
  id
  name
  setPoint
  mode
  tempProbeDetails {
    id
    name
    reading
    physAddr
  }
  heatSettings {
    gpio
  }
  coolSettings {
    gpio
  }
}